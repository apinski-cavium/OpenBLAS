
ifeq ($(CORE), ARMV8)
CCOMMON_OPT += -march=armv8-a
FCOMMON_OPT += -march=armv8-a
endif

ifeq ($(CORE), THUNDERX)
MYOPTS = -mcpu=cortex-a53 -funroll-loops
CCOMMON_OPT += $(MYOPTS)
FCOMMON_OPT += $(MYOPTS)
endif

